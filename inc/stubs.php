<?php
/*
 * Stub functions for compatibility with plugins or themes that expect these functions to exist.
 * These functions do nothing but prevent fatal errors if called.
 */

if ( false ) {
	function wp_rml_objects() {}
}